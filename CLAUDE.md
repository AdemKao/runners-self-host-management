# Claude Code

Follow the repository-wide agent instructions in [AGENTS.md](AGENTS.md).

Before making changes, inspect the live CLI contract:

```bash
bash runnerctl agent --json
```

Then run the project smoke tests:

```bash
bash tests/smoke.sh
```

Do not duplicate policy here; `AGENTS.md` is the canonical repository instruction file.
